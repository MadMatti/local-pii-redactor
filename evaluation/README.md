# Evaluation

`baselines/` will contain versioned baseline definitions. `results/` is for
generated raw predictions, metrics, and comparison reports and is ignored.
Evaluation must use the canonical prompt and frozen prepared test split.
