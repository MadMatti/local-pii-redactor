# Project commands

- `data/`: dataset download, inspection, synthetic preview, build, validation.
- `evaluation/`: future baseline and model-quality commands.
- `model/`: future training, fusion, conversion, and quantization commands.

Commands bootstrap `src/` for direct execution but an editable install remains
the supported development setup.
