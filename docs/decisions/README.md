# Decision records

Decision records capture human approvals that change project gates. Each record
states the decision, evidence reviewed, consequences, and any remaining limits.
They are immutable once used to produce a frozen artifact; later changes require
a superseding record rather than an edit that erases history.
