# Deployment

Deployment work is intentionally separate from Mac data preparation and
training. `raspberry_pi/` will contain host preparation and artifact transfer;
`systemd/` will contain service definitions after CLI and server validation.
